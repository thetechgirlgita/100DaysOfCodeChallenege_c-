//This line includes the input/output stream library, which is necessary for using std::cout
#include <iostream> 
//This line includes the C standard input/output library to print.
#include <cstdio>

using namespace std; // it allows you to use elements from the std namespace without explicitly specifying it

int main() {
    printf("Hello, World!");
    return 0;
}
